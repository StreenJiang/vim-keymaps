// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    },
  },
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // "j k": "vim::NormalBefore"
    },
  },
  {
    "context": "Editor",
    "unbind": {
      "cmd-.": "editor::ToggleCodeActions"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "cmd-.": "terminal_panel::Toggle"
    }
  },
  {
    "context": "Workspace",
    "unbind": {
      "ctrl-`": "terminal_panel::Toggle"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "unbind": {
      "p": "project_panel::Open"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "space": "project_panel::Open"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "unbind": {
      "space": "project_panel::Open"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "tab": "project_panel::Open"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w shift-k": "workspace::MovePaneUp"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w shift-j": "workspace::MovePaneDown"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w shift-h": "workspace::MovePaneLeft"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w shift-l": "workspace::MovePaneRight"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-k": "workspace::ActivatePaneUp"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w k": "workspace::ActivatePaneUp"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-j": "workspace::ActivatePaneDown"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w j": "workspace::ActivatePaneDown"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-h": "workspace::ActivatePaneLeft"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w h": "workspace::ActivatePaneLeft"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-l": "workspace::ActivatePaneRight"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w l": "workspace::ActivatePaneRight"
    }
  },
  {
    "context": "(VimControl && !menu) ||  !Terminal",
    "bindings": {
      "ctrl-c": "pane::CloseActiveItem"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w q": "pane::CloseActiveItem"
    }
  },
  {
    "context": "VimControl && !menu && (vim_mode == normal || not_editing)",
    "bindings": {
      "space c a": "editor::ToggleCodeActions"
    }
  },
  {
    "context": "VimControl && !menu",
    "unbind": {
      "g .": "editor::ToggleCodeActions"
    }
  },
  {
    "context": "Editor && (vim_mode == normal || not_editing)",
    "bindings": {
      "space e f": "project_panel::ToggleFocus"
    }
  },
  {
    "context": "Picker > Editor",
    "unbind": {
      "ctrl-h": "editor::Backspace"
    }
  },
  {
    "context": "Editor",
    "unbind": {
      "ctrl-h": "editor::Backspace"
    }
  },
  {
    "context": " vim_mode == visual || vim_mode == operator",
    "bindings": {
      "shift-k": "editor::MoveLineUp"
    }
  },
  {
    "context": "vim_mode == normal || vim_mode == visual || vim_mode == operator",
    "unbind": {
      "[ e": "editor::MoveLineUp"
    }
  },
  {
    "context": "vim_mode == normal || vim_mode == visual || vim_mode == operator",
    "bindings": {
      "shift-j": "editor::MoveLineDown"
    }
  },
  {
    "context": "vim_mode == normal || vim_mode == visual || vim_mode == operator",
    "unbind": {
      "] e": "editor::MoveLineDown"
    }
  },
  {
    "context": "((Workspace || Editor || ProjectPanel) && (vim_mode == normal || not_editing)) || (!workspace_has_open_files && !Editor && !Terminal)",
    "bindings": {
      "space f f": "file_finder::Toggle"
    }
  },
  {
    "context": "!Editor && !Terminal",
    "bindings": {
      "ctrl-n": "pane::ActivateNextItem"
    }
  },
  {
    "context": "!Editor && !Terminal",
    "unbind": {
      "] b": "pane::ActivateNextItem"
    }
  },
  {
    "context": "!Editor && !Terminal",
    "bindings": {
      "ctrl-p": "pane::ActivatePreviousItem"
    }
  },
  {
    "context": "!Editor && !Terminal",
    "unbind": {
      "[ b": "pane::ActivatePreviousItem"
    }
  },
  {
    "context": "Editor",
    "unbind": {
      "ctrl-p": "editor::MoveUp"
    }
  },
  {
    "context": "Editor",
    "unbind": {
      "ctrl-n": "editor::MoveDown"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-n": "pane::ActivateNextItem"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w ctrl-g t": "pane::ActivateNextItem"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "bindings": {
      "ctrl-p": "pane::ActivatePreviousItem"
    }
  },
  {
    "context": "(VimControl && !menu) || (!Editor && !Terminal)",
    "unbind": {
      "ctrl-w ctrl-g shift-t": "pane::ActivatePreviousItem"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "q": "project_panel::Toggle"
    }
  },
  {
    "context": "(vim_mode == helix_normal || vim_mode == helix_select || (vim_mode == normal || not_editing)) && !menu",
    "bindings": {
      "space /": "pane::DeploySearch"
    }
  },
  {
    "context": "ProjectSearchBar",
    "bindings": {
      "cmd-shift-f": "search::FocusSearch"
    }
  },
  {
    "context": "ProjectSearchBar",
    "unbind": {
      "cmd-shift-f": "search::FocusSearch"
    }
  },
  {
    "context": "((Workspace || Editor || ProjectPanel) && (vim_mode == normal || not_editing)) || (!workspace_has_open_files && !Editor && !Terminal)",
    "bindings": {
      "space /": "pane::DeploySearch"
    }
  },
  {
    "context": "Workspace",
    "unbind": {
      "cmd-shift-f": "pane::DeploySearch"
    }
  },
  {
    "context": "Editor && (vim_mode == normal || not_editing)",
    "bindings": {
      "space b": "editor::ToggleBreakpoint"
    }
  },
  {
    "context": "Editor",
    "unbind": {
      "f9": "editor::ToggleBreakpoint"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "a": "project_panel::NewFile"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "unbind": {
      "%": "project_panel::NewFile"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "bindings": {
      "d": "project_panel::Delete"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "unbind": {
      "shift-d": "project_panel::Delete"
    }
  },
  {
    "context": "ProjectPanel && not_editing",
    "unbind": {
      "d": "project_panel::NewDirectory"
    }
  },
  {
    "context": "((Workspace || Editor || ProjectPanel) && (vim_mode == normal || not_editing)) || (!workspace_has_open_files && !Editor && !Terminal)",
      "bindings": {
      "space space": "file_finder::Toggle"
    }
  },
  {
    "context": "(vim_mode == helix_normal || vim_mode == helix_select) && !menu",
    "bindings": {
      "g a r": "editor::FindAllReferences"
    }
  },
  {
    "context": "(vim_mode == helix_normal || vim_mode == helix_select) && !menu",
    "unbind": {
      "g r": "editor::FindAllReferences"
    }
  },
  {
    "context": "VimControl && !menu",
    "bindings": {
      "g r": "editor::GoToImplementation"
    }
  },
  {
    "context": "VimControl && !menu",
    "unbind": {
      "g r i": "editor::GoToImplementation"
    }
  }
]
